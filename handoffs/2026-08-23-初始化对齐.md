# 交接：初始化对齐（2026-08-23）

- 日期：2026-08-23

## 上一段对话做了什么
与 AI 对齐了 gomoku-coach 项目的协作体系：明确两个项目（AI 协作文档仓 + 引擎代码仓）、引擎的层级含义、文档仓目录结构、命名口径、人-AI 对话规范。并搭好了 `gomoku-coach-ai-collab` 仓的目录骨架与初稿文档。

## 当前进度
- 阶段 0（初始化对齐）基本完成。
- 目录骨架与 `spec/`（三篇）、`plans/`、`decisions/`（含首篇）、`handoffs/`（含本篇）、`scripts/` 初稿已建立。

## 关键结论
- `gomoku-coach-engine` 是「解说引擎」，它调用外部「胜率计算引擎」——此引擎非彼引擎。
- 文档仓目录 = `spec/ plans/ decisions/ handoffs/ scripts/`。
- 不用代号，用真实项目名。

## 待办 / 下一步
- 阶段 1：在 `gomoku-coach-engine` 中确定胜率计算引擎的接口协议并接入。
- （可选）更新 `gomoku-coach-engine/README.md` 中的旧名 `gomoku-coach` 措辞，与重命名一致。

## 必读文档
- `spec/01-项目结构总览.md`
- `spec/02-术语表.md`
- `spec/03-人-AI对话规范.md`
- `decisions/2026-08-23-目录结构与命名定调.md`

## 注意事项 / 坑
- 重命名后，engine 仓内 README 仍可能写旧名 `gomoku-coach`，后续留意统一。
- 凡出现「引擎」二字，先确认指哪一层。
